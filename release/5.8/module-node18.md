# \`--module node18\`

## TL;DR

`--module node18`  の安定した動作が提供されるようになった。

* `require()`  は `node18`  ではesmをインポートできない、`nodenext`  ならできる
* import assertionsは `node18`  で使える、`nodenext`  ではimport attributesに代わるため使えない
